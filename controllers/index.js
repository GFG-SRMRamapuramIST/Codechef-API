const getUserInfo = require("./userInfo");
const heatMap = require("./heatMap");
const ratingGraph = require("./ratingGraph");
const contestData = require("./contestData");

module.exports = { getUserInfo, heatMap, ratingGraph, contestData };
